name: Release Workflow

on:
  push:
    branches: [ "master", "dev" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore VeeamHealthCheck.csproj
      working-directory: vHC/HC_Reporting

    - name: Build
      run: dotnet build VeeamHealthCheck.csproj --configuration Release --no-restore
      working-directory: vHC/HC_Reporting

    - name: Run tests
      run: dotnet test VeeamHealthCheck.csproj --no-restore --verbosity normal
      working-directory: vHC/HC_Reporting

    - name: Code scan
      uses: github/codeql-action/init@v1
      with:
        languages: csharp

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v1

    - name: Publish project
      run: dotnet publish VeeamHealthCheck.csproj -c Release -o ./publish
      working-directory: vHC/HC_Reporting

    - name: Zip published files
      run: |
        $version = (Get-Command ./publish/VeeamHealthCheck.exe).FileVersionInfo.FileVersion
        Compress-Archive -Path ./publish/* -DestinationPath ./VeeamHealthCheck-$version.zip
      shell: pwsh

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.sha }}
        release_name: Release ${{ github.sha }}
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: VeeamHealthCheck-*.zip
        asset_name: VeeamHealthCheck.zip
        asset_content_type: application/zip